# 仿真运行记录

`sionna/` 存放当前固定基站 Stage 2 的正式标签，包括已完成的 `dac_sionna_35ghz_depth8_27360_v3`；`irt/`、`winprop/` 保存先前的 WinProp IRT 尝试与诊断。随机基站 V5 的 Sionna RT 数据在 `../02_inversion/bs_inversion_scale4_v1/`。GitHub 展示各轮实验的目录说明，以及运行目录顶层的轻量配置、进度和摘要；射线追踪输出、逐 tile 日志及数据包留在本机。本机完整目录约 21 GiB。

当前可执行脚本和配置主要在仓库根目录的 `projects/`、`scripts/` 和 `tools/`；从 [当前 Sionna 路线](../projects/irt_label_pipeline/SIONNA_CURRENT.md) 开始阅读训练流程。
