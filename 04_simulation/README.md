# 仿真运行记录

`irt/`、`sionna/`、`winprop/` 分别存放对应仿真方案的历史运行目录。GitHub 展示各轮实验的目录说明，以及运行目录顶层的轻量配置、进度和摘要；射线追踪输出、逐 tile 日志及数据包留在本机。本机完整目录约 21 GiB。

当前可执行脚本和配置主要在仓库根目录的 `projects/`、`scripts/` 和 `tools/`；这里主要用于追溯已开展的实验。
