# 目录迁移记录（2026-09-24）

主目录：`D:\桌面\dac`。原 `C:\Users\pc\Documents\大创` 的内容已完整复制到此目录，Git 历史也已复制；合并前 Git 基线为 `d51d923`。复制后用 Robocopy 只读比对，结果只有 D 盘原有的额外文件，无遗漏的 C 盘源文件。

## 原 D 盘内容

| 原根目录 | 当前位置 |
| --- | --- |
| `512mdata`、`antenna_patterns`、`raw_datasets`、`prepared_4m`、`normalized_4m_v1` | `01_data/` 下同名目录 |
| `bs_inversion_pilot_v1`、`bs_inversion_scale4_v1` | `02_inversion/` 下同名目录 |
| `models`、`experiments`、`inference_demo`、`stage1_dpm_unet_runs` | `03_runs/` 下同名目录 |
| `code`、`github code` | `05_legacy_code/` 下同名目录 |
| `开题`、`参考文献`、Scale-4 单样本评估图片 | `06_reference/` |

原根目录名保留为**隐藏的 Windows 目录连接**，旧的绝对路径仍可读取；实际文件在表中位置。原 D 盘数据没有另复制一份。旧连接是本机兼容设施，GitHub 克隆不会包含。

## 原 E 盘内容

| 原目录前缀或名称 | 当前位置 |
| --- | --- |
| `dac_irt_*` | `04_simulation/irt/` |
| `dac_sionna_*`（环境目录除外） | `04_simulation/sionna/` |
| `dac_winprop_*` | `04_simulation/winprop/` |
| `dac_stage*` | `03_runs/E_stage/` |
| `dac_bs_inversion_scale4_v1_work` | `02_inversion/E_work/` |
| `dac_tools` | `05_legacy_code/` |
| `dac_checkpoints` | `07_backup/` |
| `dac_sionna_rt_env` | `08_runtime/` |

33 个 E 盘目录已复制到上述实际位置，随后逐目录执行 Robocopy 只读比对，33 项均返回无差异。原件保存在 `E:\archive_dac_before_move_20260924/`，原 E 盘路径改为指向 D 盘的隐藏目录连接。保留备份是为了迁移后核查，不参与当前工作路径。

## 代码及当前状态

- 当前代码与配置中的原 C/D/E 盘路径已改为 `D:\桌面\dac` 下对应路径；`tools/rewrite_dac_paths.py` 记录了替换规则。历史训练结果和佐证材料的来源校验清单保持原样。
- 原 `C:\Users\pc\Documents\大创` 仍保留一份复制前的内容：该文件夹被当前应用占用，不能安全重命名。**继续修改项目时请使用 `D:\桌面\dac`**；不要把 C 盘旧副本当作最新工作目录。
- `08_runtime/dac_sionna_rt_env/Scripts/python.exe` 能启动，`sionna` 包可被发现；该环境的原位置也没有 `torch` 包，需按具体任务另行使用对应训练环境。
- 对配置中的 148 个主要输入路径做存在性检查，144 个可找到。另 4 个指向此前未完成的两轮 WinProp Stage2 运行所需的归一化文件或 Stage2A 模型，原轮次没有这些文件；迁移未补造结果。`tools/clean_beijing_wallman.py` 的可选 OSM 参考 `buildings.shp` 也不在现有数据中。
- 文件夹整理及代码路径修改后，项目代码、目录说明和筛选后的轻量记录已推送到 GitHub；大型数据、权重与运行环境仍留在本机。
